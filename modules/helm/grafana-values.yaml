adminPassword: "admin123"

service:
  type: LoadBalancer

persistence:
  enabled: true
  size: 2Gi
  storageClassName: gp2

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      access: proxy
      url: http://prometheus-server.monitoring.svc.cluster.local
